const mongoose = require('mongoose');
const Lesson = require('../models/Lesson');
const Roadmap = require('../models/Roadmap');

const seed = async () => {
  console.log('Seed data system ready.');
};

module.exports = seed;
